<div class ="container">
  <div class="search-index">
    <form novalidate="novalidate" class="simple_form search" action="/" accept-charset="UTF-8" method="get"><input name="utf8" type="hidden" value="&#x2713;" />
<<<<<<< HEAD
      <div class="search-form-control form-group">
        <%# <input class="form-control string required" type="text" name="search[query]" id="search_query" /> %>
        <%# input of the search ActiveRecord %>
        <%= form_tag plants_path, method: :get do %>   
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
        placeholder: "Find a plant"
        %>
        <%= submit_tag "Search", class: "btn btn-primary" %>
        <% end %>
        <%# input of the search ActiveRecord %>
        <%# <button name="button" type="submit" class="btn btn-flat">
          <i class="fas fa-search"></i> Search
        </button> %>
      </div>
    </form>
=======
        <div class="search-form-control form-group">
          <input class="form-control string required" type="text" name="search[query]" id="search_query" />
          <button name="button" type="submit" class="btn btn-flat">
            <i class="fas fa-search"></i> Search
          </button>
        </div>
      </form>
>>>>>>> e9aa7aa9f9866feb6f1d07e93b9330d311bc2930
  </div>

  <div class="row">
    <div class="col-lg-12">
      <div class="row">


        <% @plants.each do |plant| %>
          <div class="col-4">
            <%= link_to(plant_path(plant), class:"card-trip") do %>

              <% if plant.photo.attached? %>
                <%= cl_image_tag plant.photo, height: 300, width: 400, crop: :fill %>
              <% else %>
                <%= image_tag asset_url('plant-icon.jpeg') %>
              <% end %>

              <div class="card-trip-infos">
                <div>
                  <p class="title"><%= plant.species %></p>
                  <p><%= plant.status %></p>
                </div>
                <h2 class="card-trip-pricing"><%= plant.pricing %>€/month</h2>
                <!-- <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" /> -->
                <div>
                  <% if plant.user.photo.attached? %>
                    <%= cl_image_tag plant.user.photo, height: 38, width: 38, class: "card-trip-user avatar-bordered"%>
                  <% else %>
                      <%= cl_image_tag asset_url('user-icon.png'), height: 38, width: 38, class: "card-trip-user avatar-bordered"%>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


</div>
